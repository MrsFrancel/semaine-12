
  # Build MatchU SaaS App

  This is a code bundle for Build MatchU SaaS App. The original project is available at https://www.figma.com/design/ReD1lYDqD97i0icHMhA42R/Build-MatchU-SaaS-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  